import { Container, Typography } from '@mui/material';

export default function AdminDashboard() {
  return (
    <Container>
      <Typography variant="h4">Dashboard Admin</Typography>
      <Typography>Aceasta este pagina pentru Admin. (în dezvoltare)</Typography>
    </Container>
  );
}
