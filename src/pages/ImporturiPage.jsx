import { Container, Typography } from '@mui/material';

export default function ImporturiPage() {
  return (
    <Container>
      <Typography variant="h4">Importuri</Typography>
      <Typography>Aceasta este pagina Importuri. (în dezvoltare)</Typography>
    </Container>
  );
}
